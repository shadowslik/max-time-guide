# Что должно быть в проекте (бот + мини-апп + бэкенд)

Отметки: ✅ — готово, ⬜ — ещё нет.

---

## Бот (aiomax) — `backend/app/bot/`

- ✅ Подключение к MAX по токену, сертификаты Минцифры (`use_certificate=True`)
- ✅ Приветствие с обращением по имени (`on_bot_start`)
- ✅ Команда `/route` — показать кнопку
- ✅ Кнопки: «Собрать маршрут» (`WebAppButton`) и «Как пользоваться» (callback)
- ✅ Ответ на callback «как пользоваться»
- ⬜ Создать кнопку для уточнения точного местоположения пользователя
- ⬜ Починить импорты и стабильный запуск через `main.py`
- ⬜ Проверить открытие мини-аппа кнопкой (после привязки URL организаторами)

Бот только открывает мини-апп. Маршруты он НЕ считает.

---

## Мини-приложение (React + Vite) — `frontend/`

- ✅ Каркас Vite + деплой на Cloudflare (стабильный HTTPS)
- ⬜ Экран ввода: город, точка старта, доступное время, интересы, фильтр «бесплатные»
- ⬜ Экран результата: список мест по порядку, тайминг, переходы, статус «успеешь вернуться»
- ⬜ Запрос на бэкенд `POST /route` и отрисовка ответа
- ⬜ Подключить MAX Bridge (контекст пользователя, платформа) с заглушкой для браузера
- ⬜ Загрузка списков из API (города, точки старта, интересы) в выпадающие поля

---

## Бэкенд API (FastAPI) — `backend/app/` (ещё НЕ начат)

- ⬜ Создать FastAPI-приложение (`main.py` → `app`), `app/__init__.py`
- ⬜ Настроить CORS (фронт с Cloudflare/localhost должен ходить на API)
- ⬜ Включить сервис `api` в `docker-compose.yml` (порт 8000)

### Ручки (endpoints)

- ⬜ **`POST /route`** — главная. Принимает город, точку старта, доступное время,
  интересы, `free_only`. Отдаёт готовый упорядоченный маршрут (места + тайминг +
  статус возврата). Внутри вызывает алгоритм подбора.
- ⬜ **`GET /health`** — проверка живости (`{"status": "ok"}`), для Docker.

## Алгоритм и данные — `backend/app/`

- ⬜ Схемы данных — контракт между API и алгоритмом
- ⬜ Оценка времени в пути (расстояние / средняя скорость, помечена как модельная)
- ⬜ Алгоритм подбора маршрута:
  - фильтр (открыто в окне времени / подходит по интересам / бесплатно при `free_only`)
  - жадный выбор по `приоритет / (время в пути + время осмотра)`
  - учёт бюджета времени + буфер на возврат
  - расчёт тайминга каждого пункта и итогового статуса

---

## Инфраструктура

- ✅ Dockerfile для backend и frontend
- ✅ `docker-compose.yml` (сейчас: bot + frontend)
- ✅ `.env.example`, `.gitignore`, `.dockerignore`
- ⬜ Включить `api` в compose, когда появится FastAPI
- ⬜ Поднять бэкенд на публичном HTTPS (иначе Cloudflare-фронт до него не достучится)
- ---
